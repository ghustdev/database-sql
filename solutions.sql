
-- -- Exercise 2 - Customer Address

-- CREATE TABLE locations (
--     id SERIAL PRIMARY KEY,
--     locations_date DATE NOT NULL,
--     id_customers INT NOT NULL,
--     FOREIGN KEY (id_customers) REFERENCES customers(id)
-- );
